<div class="modal fade" id="<%= "institution-#{institution.id}" %>">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title"><%= t('alert.warning') %></h4>
      </div>
      <div class="modal-body">
        <p>
          <strong>
            <%= "#{t('action.destroy').capitalize} #{institution.name}!" %>
          </strong>
        </p>

        <p><%= t('question.are_you_sure') %></p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal"><%= t('option.n') %></button>
        <%= link_to t('option.y'), institution, method: :delete, class: 'btn btn-danger' %>
      </div>
    </div>
  </div>
</div>