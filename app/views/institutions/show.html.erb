<div class="container">
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="#"><%= t('model.institution') %></a>
        <ul class="nav navbar-nav brand">
          <li><%= link_to t('action.edit'), edit_institution_path(@institution) %></li>
          <li><%= link_to t('action.back'), institutions_path %></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="row">
    <div class="col-md-12">
      <p>
        <strong><%= t('head.name') %>:</strong>
        <%= @institution.name %>
      </p>

      <p>
        <strong><%= t('head.description') %>:</strong>
        <%= @institution.description %>
      </p>
    </div>
  </div>
</div>