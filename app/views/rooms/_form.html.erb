<div class="row">
  <div class="col-md-6">
    <%= form_for(@room, html: {class: 'from'}) do |f| %>

      <% if @room.errors.any? %>
        <div id="error_explanation">
          <ul>
          <% @room.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="input-control">
        <%= f.label :name %><br>
        <%= f.text_field :name, class: 'form-control', autofocus: true %>
      </div>

      <div class="input-control">
        <%= f.label :description %><br>
        <%= f.text_area :description, rows: 5, class: 'form-control' %>
      </div>

      <div class="input-control">
        <%= f.label :permission %><br>
        <%= f.collection_select :permission_id, Permission.all, :id, :name, {prompt: true},  {class: 'form-control'} %>
      </div>

      <div class="input-control">
        <%= f.label :floor %><br>
        <%= f.collection_select :floor_id, Floor.all, :id, :name, {prompt: true},  {class: 'form-control'} %>
      </div>
    <% end %>
  </div>
</div>

<script type="text/javascript">
  $("#save").click(function(){
    $("form:first").submit();
  });
</script>